# genhuman開發規則

## 🚨 **第負一優先級：需求確認原則（2025-01-11新增）**

### 📋 **需求確認強制流程**
**🚨 最高優先級規則**：在開始任何開發工作之前，必須先與用戶確認需求

#### **執行標準**：
1. **禁止直接開始工作** - 收到任務後不得立即開始編碼
2. **必須先理解需求** - 仔細分析用戶的真實需求和業務邏輯
3. **主動確認細節** - 向用戶確認不明確的需求點
4. **獲得明確同意** - 得到用戶確認後才能開始開發
5. **記錄確認結果** - 將確認的需求記錄在開發日誌中

#### **需求確認模板**：
```
我理解您的需求是：
1. [具體需求1]
2. [具體需求2]
3. [具體需求3]

請確認以下理解是否正確：
- [關鍵點1確認]
- [關鍵點2確認]
- [關鍵點3確認]

確認無誤後我將開始開發工作。
```

#### **違反後果**：
- ❌ **開發返工** - 需求理解錯誤導致重新開發
- ❌ **時間浪費** - 錯誤方向的開發完全無效
- ❌ **用戶不滿** - 交付結果不符合期望

## 🚨 **第零優先級：v2.1開發原則（基於失敗教訓）**

### 📚 **開發前必讀錯誤檔案**
**🚨 強制要求**：每次開始開發任務前，必須先閱讀 `genhuman開發錯誤.md`
- **目的**：了解過往所有錯誤案例，避免重複犯錯
- **重點關注**：錯誤原因、解決方案、預防措施
- **執行標準**：不看錯誤檔案不准開始開發

### 📍 **v1.0 & v2.0失敗原因分析**
- ❌ **過度複雜的修改導致502錯誤** - 一次修改太多組件
- ❌ **同時修改多個系統組件** - 缺乏隔離性
- ❌ **引入不必要的依賴（GuzzleHttp等）** - 增加系統複雜度
- ❌ **沒有遵循最小修改原則** - 範圍過大難以排錯
- ❌ **v2.0 Phase 1全面失敗** - 一次性添加太多新功能和文件

### 🎯 **v2.1核心原則（必須嚴格遵循）**
- ✅ **超小步修改原則** - 每次只修改1-2個文件，立即驗證
- ✅ **零依賴策略** - 不引入新的Composer依賴，不增加系統複雜度
- ✅ **簡化邏輯原則** - 避免複雜的認證和數據庫操作
- ✅ **漸進式驗證** - 每30分鐘為一個開發週期，立即測試
- ✅ **隨時回滾準備** - 每步都有回滾方案，確保穩定狀態
- ✅ **避免終端卡頓原則** - 優先直接修改文件，避免頻繁使用終端指令
- ✅ **高效協作模式** - 採用人機協作方式，明確分工執行

### 💰 **修復錯誤成本控制**
**核心理念**：我們情願每一步都小步穩健的開發，修改錯誤之後，修復錯誤的成本會指數級增長。

**成本分析**：
- **小步修改成本** = 15分鐘修改 + 15分鐘驗證 = 30分鐘
- **大步修改失敗成本** = 2小時修改 + 4小時排錯 + 2小時回滾 = 8小時
- **穩健開發投資回報率** = 800% (8小時 ÷ 1小時 × 10次修改)

**執行標準**：
1. **每次修改前評估影響範圍** - 如果超過1個文件，考慮拆分
2. **每次修改後立即驗證** - 不積累問題到最後解決
3. **遇到複雜問題立即停止** - 重新評估，採用更簡單方案
4. **保持可回滾狀態** - 隨時可以回到上一個穩定版本

### 🔧 **避免終端卡頓策略（新增重要規則）**

#### **問題識別**：
- ❌ **終端指令經常卡住** - 降低開發效率
- ❌ **Git操作容易中斷** - 影響部署流程
- ❌ **長時間等待響應** - 浪費開發時間

#### **解決方案**：

**1. 優先直接修改文件方案**：
```
✅ 好的方式：
🔧 立即修復：更新IndexController跳轉路徑
🔧 創建直接跳轉頁面（避免複雜路由問題）
🎉 完美解決！避免終端卡住
```

**2. 人機協作模式**：
```
✅ AI負責：
- 代碼分析和修改
- 文件創建和編輯
- 技術方案設計
- 測試URL提供

✅ 用戶負責：
- Git操作（add, commit, push）
- 終端指令執行
- 部署確認
- 最終測試驗證
```

#### **標準協作流程**：

**步驟1：AI完成代碼修改**
```
🔧 直接修改相關文件
📝 提供清晰的修改說明
🎯 準備測試驗證URL
```

**步驟2：用戶執行部署**
```bash
cd D:\genhuman\genhuman
git add .
git commit -m "[具體修改說明]"
git push origin main
```

**步驟3：協作驗證**
```
⏰ AI提供時間預估
📋 AI提供測試步驟
🔍 用戶執行驗證測試
✅ 雙方確認完成狀態
```

#### **強制執行規則**：
1. **禁止AI執行Git操作** - 全部由用戶手動完成
2. **禁止AI使用複雜終端指令** - 優先文件修改方案
3. **必須提供部署指令** - 清晰的bash命令塊
4. **必須提供測試URL** - 具體的驗證步驟
5. **必須預估時間** - 幫助用戶管理期望

---

## 🚨 第零優先級：真實時間管理規則

### 📍 規則0: 強制使用真實時間
**目的**：確保所有時間記錄準確，避免時間混亂

#### 🕐 時間設置要求：

**嚴格規則**：
- **所有時間必須基於真實當前時間**
- **禁止使用虛假或隨意設置的時間**
- **時間格式統一使用24小時制：HH:MM**
- **日期格式統一使用：YYYY-MM-DD**

**執行標準**：
1. **獲取當前時間**：每次設置時間前，必須先確認當前真實時間
2. **時間計算準確**：預計完成時間 = 當前時間 + 預計執行時間
3. **時間記錄一致**：開始時間、檢查時間、完成時間必須邏輯一致
4. **禁止時間倒流**：後續時間不能早於前序時間

---

## 🚨 第一優先級：前端開發效率保證

### 📍 規則1: 強制開發任務監控機制
**目的**：防止開發任務卡住，確保開發效率

#### 🔧 執行流程（每次開發任務必須遵循）：

**步驟1：任務前聲明**
```javascript
// 使用真實系統時間：2025-07-12 當前時間
console.log(`⏰ [任務名稱] 預計時間：X分鐘 - 開始時間：${getCurrentTime()} - 預計完成時間：${getEndTime()}`);
console.log(`📋 執行內容：具體要做什麼`);
console.log(`🎯 成功標準：什麼情況算完成`);
```

**步驟2：執行開發任務**
- 代碼修改
- 功能測試
- 界面調試
- API對接

**步驟3：任務完成確認**
```javascript
console.log(`✅ [${getCurrentTime()}] 任務完成確認`);
console.log(`📊 實際用時：${actualTime}分鐘`);
console.log(`🔍 完成質量：功能正常/需要優化`);
```

---

## 📋 第二優先級：核心開發規範

### 🔄 版本控制與部署規則

#### A. GitHub同步規則
- **每完成一次功能修改，都要同步到GitHub**
- **正確的Git倉庫**: https://github.com/andyyen817/GenHuman.git
- **工作目錄**: 必須在 `front` 目錄下執行所有Git操作
- **Git操作流程**:
  1. `cd front` (切換到正確目錄)
  2. `git add .` (添加所有變更)
  3. `git commit -m "[功能模塊] 具體修改內容"` (提交變更)
  4. `git push origin master` (推送到正確倉庫)
  5. **🔍 檢查GitHub檔案狀態** (新增必要步驟)
- 提交信息格式：`[功能模塊] 具體修改內容`
- 例如：`[AI聊天] 實現異步接口對接功能`

#### A1. GitHub檔案檢查規則
- **每次推送到GitHub後，必須檢查檔案是否有問題**
- **檢查方法**:
  1. 使用GitHub API檢查最新commit狀態
  2. 檢查是否有語法錯誤或衝突
  3. 確認所有檔案都正確上傳
- **如果發現問題**:
  1. 立即修復檔案錯誤
  2. 重新提交修復版本
  3. 再次檢查直到沒有問題

#### B. Zeabur部署規則
- **每次修改後都要部署到Zeabur前端環境**
- **Zeabur項目**: genhuman-digital-human
- **確保部署到正確的項目**: genhuman-digital-human
- **部署前檢查**: 確認當前目錄結構正確
- **重要**: Git操作必須在 `D:\genhuman\genhuman` 目錄下執行

### ⏰ 任務管理規範

#### C. 任務時間預設規則
- **執行任務前，需要先預設這個任務需要執行多長時間**
- 估算時間包括：分析時間 + 編碼時間 + 測試時間 + 調試時間
- 例如：`[預估時間] AI聊天異步對接 - 3小時（分析30分鐘+編碼120分鐘+測試30分鐘）`

#### D. 任務完成檢查規則
- **每完成一個任務，設計檢查腳本，檢查是否完成任務**
- 檢查腳本包括：功能測試、界面測試、用戶體驗測試
- 檢查結果記錄：成功/失敗 + 具體測試結果

#### E. 執行前規則參考
- **每一次執行任務前，先參考前端開發規則檔案**
- 確保遵循所有開發規範
- 檢查清單：時間預設✓、任務管理✓、檢查腳本✓、同步部署✓、語法驗證✓、部署驗證✓

#### F. 強制部署驗證清單 🚨 **每次任務完成後必須執行**
**任務完成的定義**：不僅是代碼修改完成，還必須包含以下所有步驟：

1. **🔍 代碼語法檢查** - 確保無語法錯誤
2. **📤 Git同步** - 推送到GitHub並檢查文件狀態
3. **🚀 Vercel部署** - 自動觸發部署並等待完成
4. **✅ 部署狀態檢查** - 驗證zeabur部署成功
5. **🌐 生產環境測試** - 訪問https://genhuman-digital-human.zeabur.app/驗證功能
6. **🔧 錯誤處理** - 如有失敗立即修復並重新部署

#### G. 部署失敗處理流程 🚨 **立即執行，不可延遲**
**如果部署失敗**：
1. **🔍 立即檢查構建日誌** - 分析錯誤原因
2. **🔧 修復代碼問題** - 解決語法錯誤或依賴問題
3. **✅ 本地驗證** - 確保修復成功
4. **🚀 重新部署** - Git推送觸發新的部署
5. **🌐 驗證成功** - 確認生產環境正常工作

### 🔧 開發流程（強化版）
1. **參考開發規則** → **預設任務時間** → **執行開發任務** → **🔍功能測試** → **🔍語法檢查** → **Git提交** → **🔍檢查GitHub檔案** → **🔍zeabur部署狀態** → **🔍生產環境驗證** → **📋任務完成確認**

2. **🚨 強制檢查點（不可跳過）**：
   - **代碼修改後**:  語法檢查 + 功能測試
   - **Git提交前**: 確保構建成功 + 所有功能正常
   - **推送GitHub後**: 檢查文件狀態 + 部署觸發確認
   - **部署完成後**: 訪問生產環境 + 驗證所有功能
   - **任務完成前**: 生產環境用戶體驗測試通過

3. **🚨 任務完成的真正定義**：
   - ❌ **錯誤定義**: 代碼修改完成 = 任務完成
   - ✅ **正確定義**: 生產環境功能驗證通過 = 任務完成

### 🌐 環境架構規則

#### F. zeabur服務器環境開發規則
**重要**：我們使用zeabur 雲服務器開發，必須遵循以下規則：

**🚀 Zeabur部署配置**：
- **服務名稱**: `genhuman-digital-human`
- **部署地址**: https://genhuman-digital-human.zeabur.app
- **容器環境**: Docker基於PHP 8.1-cli
- **工作目錄**: `/var/www/html/server`
- **啟動命令**: `php start.php start`（前台模式）

**📋 環境變量配置**：
```bash
# 必須設置的環境變量
WEBMAN_MODE=container
DB_HOST=mysql.zeabur.internal
DB_DATABASE=zeabur
DB_USERNAME=root
DB_PASSWORD=fhlkzgNuRQL79C5eFb4036vX2T18YdAn
```

**🔧 PHP擴展要求**：
- `pdo_mysql` - 數據庫連接
- `zip` - 文件壓縮
- `bcmath` - 數學計算
- `gd` - 圖片處理
- `mbstring` - 多字節字符串
- `pcntl` - 進程控制（Webman必需）

**⚠️ 部署注意事項**：
1. **容器兼容性**: 必須使用前台模式啟動，避免守護進程
2. **文件權限**: `server/runtime` 目錄需要777權限
3. **依賴管理**: 使用`composer install --no-dev --optimize-autoloader`
4. **監控機制**: 通過Zeabur控制台監控容器狀態
5. **日誌查看**: 通過Zeabur日誌功能排查問題

**🚨 部署失敗處理**：
- **502錯誤**: 檢查啟動命令和容器配置
- **崩潰重試**: 檢查PHP擴展和環境變量
- **連接超時**: 檢查數據庫連接配置

#### G. zeabur mysql數據庫規則
**重要**：我們使用zeabur mysql雲數據庫開發，必須遵循以下規則：

**📊 數據庫配置信息**：
- **主機地址**: `mysql.zeabur.internal` (內網地址)
- **數據庫名**: `zeabur`
- **用戶名**: `root`
- **密碼**: `fhlkzgNuRQL79C5eFb4036vX2T18YdAn`
- **端口**: `3306` (默認)
- **字符集**: `utf8mb4`

**🔐 連接安全規則**：
1. **內網訪問**: 只能從Zeabur服務內部訪問
2. **密碼管理**: 密碼存儲在環境變量中
3. **連接池**: 使用Think-ORM連接池管理
4. **備份策略**: 依賴Zeabur自動備份

**📋 數據庫操作規範**：
- **表前綴**: 統一使用 `yc_` 前綴
- **字段命名**: 使用小寫字母和下劃線
- **索引策略**: 重要查詢字段必須建立索引
- **數據類型**: 優先使用合適的數據類型節省空間

**🔧 開發工具連接**：
```php
// Think-ORM配置示例 (config/think-orm.php)
'connections' => [
    'mysql' => [
        'type' => 'mysql',
        'hostname' => $_ENV['DB_HOST'] ?? 'mysql.zeabur.internal',
        'database' => $_ENV['DB_DATABASE'] ?? 'zeabur',
        'username' => $_ENV['DB_USERNAME'] ?? 'root',
        'password' => $_ENV['DB_PASSWORD'] ?? 'fhlkzgNuRQL79C5eFb4036vX2T18YdAn',
        'hostport' => '3306',
        'charset' => 'utf8mb4',
        'prefix' => 'yc_',
    ]
]
```

**⚠️ 數據庫注意事項**：
1. **遷移管理**: 使用SQL腳本進行數據庫結構變更
2. **性能監控**: 定期檢查慢查詢日誌
3. **數據安全**: 敏感數據必須加密存儲
4. **版本控制**: 數據庫結構變更必須版本化管理

#### H. 前後端協作規則
**github + zeabur+zeabur mysql 環境下的協作要求**：

**🔄 Git工作流程**：
1. **倉庫地址**: https://github.com/andyyen817/GenHuman.git
2. **工作目錄**: `D:\genhuman\genhuman` (根據實際用戶路徑)
3. **分支策略**: 
   - `main/master`: 生產環境分支
   - `develop`: 開發分支
   - `feature/*`: 功能開發分支

**📤 代碼提交規範**：
```bash
# 標準提交流程
cd D:\genhuman\genhuman  # 切換到項目根目錄
git add .
git commit -m "[模塊] 具體修改內容"
git push origin main
```

**🚀 自動部署流程**：
1. **GitHub Push** → 2. **Zeabur自動構建** → 3. **容器重新部署** → 4. **健康檢查**

**🔧 前端開發規則**：
- **框架**: Vue3 + TypeScript
- **構建工具**: Vite
- **UI庫**: Element Plus
- **API調用**: Axios
- **狀態管理**: Pinia

**⚙️ 後端開發規則**：
- **框架**: Webman (基於Workerman)
- **ORM**: Think-ORM
- **API設計**: RESTful API
- **認證**: JWT Token
- **中間件**: 跨域、認證、日誌

**🔗 前後端聯調規則**：
1. **API文檔**: 使用統一的API文檔格式
2. **接口規範**: 統一響應格式
3. **錯誤處理**: 統一錯誤碼和錯誤信息
4. **數據驗證**: 前後端雙重驗證

**📊 協作監控**：
- **部署狀態**: 通過Zeabur控制台監控
- **API性能**: 記錄響應時間和錯誤率
- **數據庫性能**: 監控查詢性能
- **用戶體驗**: 前端錯誤監控

### 📁 項目結構確認（更新：Vidspark分離架構）
```
D:\genhuman\genhuman\                    # 項目根目錄
├── server/                              # 後端PHP代碼
│   ├── app/                            # 應用核心代碼
│   │   ├── admin/                      # GenHuman管理後台
│   │   ├── api/                        # GenHuman API接口
│   │   ├── common/                     # 公共類和工具
│   │   ├── model/                      # 數據模型
│   │   └── service/                    # 業務服務
│   ├── config/                         # 配置文件
│   │   ├── think-orm.php              # 數據庫配置
│   │   ├── server.php                 # 服務器配置
│   │   ├── route.php                  # 路由配置（含Vidspark路由）
│   │   └── app.php                    # 應用配置
│   ├── public/                         # 靜態文件目錄
│   │   ├── admin/                     # GenHuman管理後台構建產物
│   │   ├── h5/                        # GenHuman H5構建產物
│   │   ├── vidspark/                  # 🆕 Vidspark用戶端構建產物
│   │   └── vidspark-admin/            # 🆕 Vidspark管理後台構建產物
│   ├── runtime/                        # 運行時文件
│   ├── vendor/                         # Composer依賴
│   └── start.php                      # 啟動文件
├── admin/                              # GenHuman管理後台前端
│   ├── src/                           # Vue源代碼
│   └── vite.config.ts                # 構建配置
├── vidspark/                           # 🆕 Vidspark用戶端前端
│   ├── src/                           # Vue源代碼
│   │   ├── i18n/                     # 獨立多語言系統
│   │   ├── api/                      # GenHuman API封裝
│   │   ├── stores/                   # Pinia狀態管理
│   │   └── views/                    # 頁面組件
│   ├── package.json                   # NPM依賴配置
│   └── vite.config.ts                # 構建到/vidspark/
├── vidspark-admin/                     # 🆕 Vidspark管理後台前端
│   ├── src/                           # Vue源代碼
│   ├── package.json                   # NPM依賴配置
│   └── vite.config.ts                # 構建到/vidspark-admin/
├── sql/                                # 數據庫腳本
├── docs/                               # 項目文檔（含分離策略文檔）
├── Dockerfile                          # Docker配置
└── README.md                           # 項目說明
```

**📋 關鍵目錄說明**：
- **server/**: PHP後端代碼，使用Webman框架（GenHuman API + 新路由）
- **admin/**: GenHuman管理後台Vue應用（保留）
- **vidspark/**: 🆕 Vidspark用戶端Vue應用（獨立）
- **vidspark-admin/**: 🆕 Vidspark管理後台Vue應用（獨立）
- **server/public/**: 所有前端應用的構建輸出目錄
- **sql/**: 數據庫相關腳本
- **docs/**: 開發文檔和API文檔

### 🚨 **頁面空白問題診斷與修復規則**

#### **問題識別**
當遇到頁面空白問題時，按以下順序排查：

1. **基礎檢查**：
   - 檢查瀏覽器控制台錯誤（F12 → Console）
   - 檢查Network標籤是否有失敗的請求
   - 確認HTML基礎結構是否正確加載

2. **Vue應用診斷**：
   - 創建簡單的診斷頁面測試Vue是否能正常啟動
   - 檢查main.js、App.vue、router等核心文件
   - 確認依賴是否正確安裝和加載

3. **API對接問題**：
   - 檢查API端點是否存在（404錯誤）
   - 確認API響應格式是否符合前端預期
   - 檢查認證token是否有效

#### **修復策略**
1. **漸進式回滾**：先回滾到最後一個穩定版本
2. **創建診斷工具**：添加基礎測試頁面確認問題範圍
3. **逐步修復**：從最基礎的功能開始修復
4. **API對接修復**：參考最新的API文檔進行對接

#### **預防措施**
- 每次重大修改前創建備份分支
- 使用漸進式部署，避免一次性大量修改
- 建立完整的錯誤監控和日誌系統
- 定期檢查API文檔與實際實現的一致性

#### i. 項目路徑配置規則 🚨 **重要事項**
**AI系統工作目錄配置**：
1. **路徑確認**: 執行任務前必須確認用戶的實際項目路徑
2. **動態配置**: 根據用戶提供的路徑動態調整工作目錄
3. **路徑驗證**: 執行命令前驗證目錄是否存在
4. **錯誤處理**: 如果路徑不存在，要求用戶提供正確路徑

**常見路徑格式**：
- Windows: `D:\項目文件夾\genhuman\genhuman
- 絕對路徑: 完整的磁盤路徑

**執行流程**：
1. 詢問用戶當前項目路徑
2. 驗證路徑是否存在
3. 設置正確的工作目錄
4. 執行Git和npm命令

#### I. 文檔生成規則 🚨 **關鍵要求**
**目的**：確保所有需要生成的.md文檔都真實創建為文件

**強制規則**：
1. **真實文檔創建**：當提到要產生 `xxxxx.md` 時，必須真實創建該文檔文件
2. **禁止僅提供文字**：不能只給出文檔內容的文字描述，必須使用write工具創建實際文件
3. **文檔完整性**：創建的文檔必須包含完整的內容結構和格式
4. **文件命名規範**：使用準確的文件名，包含版本號和日期標識

**執行流程**：
1. 分析用戶需求，確定需要創建的文檔
2. 使用write工具創建實際的.md文件
3. 確保文檔內容完整、格式正確
4. 向用戶確認文檔已成功創建

#### J. Vidspark項目分離開發規則 🚨 **新增規則**
**目的**：確保Vidspark項目與GenHuman項目的清晰分離和有效協作

**分離原則**：
1. **獨立項目結構**：Vidspark項目完全獨立於GenHuman項目
2. **API復用策略**：復用GenHuman API但使用新的封裝層
3. **路由分離**：不同應用使用不同的路由前綴
4. **構建分離**：各項目獨立構建到不同目錄

**開發流程**：
1. **Vidspark用戶端**：在`vidspark/`目錄開發，構建到`server/public/vidspark/`
2. **Vidspark管理後台**：在`vidspark-admin/`目錄開發，構建到`server/public/vidspark-admin/`
3. **API封裝**：在`vidspark/src/api/`中封裝GenHuman API調用
4. **多語言系統**：使用獨立的Vue I18n配置，不與GenHuman衝突

**部署規則**：
- Vidspark用戶端：`https://domain.com/vidspark/`
- Vidspark管理後台：`https://domain.com/vidspark-admin/`
- GenHuman系統：保持原有路徑不變

**避免衝突**：
1. **命名空間分離**：使用不同的CSS類名前綴和組件名稱
2. **狀態管理分離**：Pinia store使用不同的模塊名稱
3. **本地存儲分離**：使用`vidspark_`前綴的localStorage鍵名
4. **路由分離**：使用不同的路由配置和守衛

#### K. 環境變數統一命名規範 🚨 **重要規範**
**目的**：統一所有環境變數命名，避免混亂和重複

**核心命名原則**：
1. **分類前綴**：根據功能區域使用統一前綴
2. **全大寫**：所有環境變數使用大寫字母
3. **下劃線分隔**：使用下劃線連接單詞
4. **語義明確**：名稱要清楚表達變數用途

**Vidspark專用環境變數分類**：

**PHP運行配置（PHP_前綴）**：
```bash
PHP_UPLOAD_MAX_FILESIZE=50M        # PHP文件上傳大小限制
PHP_POST_MAX_SIZE=100M             # PHP POST數據大小限制
PHP_MEMORY_LIMIT=256M              # PHP內存限制
PHP_MAX_EXECUTION_TIME=300         # PHP最大執行時間
PHP_MAX_INPUT_TIME=300             # PHP輸入時間限制
PHP_MAX_FILE_UPLOADS=20            # PHP最大文件上傳數量
```

**Vidspark應用配置（VIDSPARK_前綴）**：
```bash
VIDSPARK_ENABLED=true                      # Vidspark功能啟用
VIDSPARK_ENVIRONMENT=production            # Vidspark運行環境
VIDSPARK_EIGHT_STEPS_ENABLED=true         # 八步法啟用
VIDSPARK_MAX_RETRY_ATTEMPTS=5             # 最大重試次數
VIDSPARK_POLLING_INTERVAL=5000            # 輪詢間隔(毫秒)
VIDSPARK_TIMEOUT_MINUTES=10               # 超時時間(分鐘)
```

**GenHuman API配置（GENHUMAN_前綴）**：
```bash
GENHUMAN_API_BASE=https://api.yidevs.com            # GenHuman API基礎地址
GENHUMAN_PRODUCTION_TOKEN=08D7EE7F91D258F27B...    # GenHuman生產Token
GENHUMAN_CALLBACK_BASE_URL=https://...             # 回調基礎URL
GENHUMAN_WEBHOOK_SECRET=vidspark_webhook_2025      # Webhook密鑰
```

**數據庫配置（DB_前綴）**：
```bash
DB_CONNECTION=mysql                        # 數據庫類型
DB_HOST=mysql.zeabur.internal             # 數據庫主機
DB_PORT=3306                              # 數據庫端口
DB_DATABASE=genhuman_db                   # 數據庫名稱
DB_USERNAME=root                          # 數據庫用戶名
DB_PASSWORD=fhlkzgNuRQL79C5eFb4036vX2T   # 數據庫密碼
```

**存儲配置（STORAGE_前綴）**：
```bash
STORAGE_PATH=/var/www/html/server/public/vidspark/storage  # 存儲路徑
STORAGE_CDN_DOMAIN=https://genhuman-digital-human.zeabur.app  # CDN域名
```

**強制同步規則**：
1. **新增環境變數**：必須同時更新`@vidspark-readme.md`
2. **命名檢查**：所有新變數必須遵循命名規範
3. **分類歸檔**：按功能分類整理到對應前綴
4. **文檔更新**：每次環境變數變更都要更新文檔

#### L. 強制控制台日誌規則 🚨 **新增關鍵規則**
**目的**：確保所有開發都有詳細的診斷日誌，提高排錯效率

**強制要求**：
1. **每個關鍵操作都要有控制台日誌**
   ```javascript
   console.log(`[${new Date().toLocaleTimeString()}] 🚀 操作開始: ${操作名稱}`);
   console.log(`[${new Date().toLocaleTimeString()}] 📊 關鍵參數: ${參數詳情}`);
   console.log(`[${new Date().toLocaleTimeString()}] ✅ 操作結果: 成功=${成功狀態}`);
   ```

2. **文件上傳必須記錄的信息**：
   - 文件名、大小、類型
   - 上傳方式（FormData/Base64）
   - 上傳響應狀態和結果
   - 生成的文件URL
   - 外部可訪問性測試結果

3. **API調用必須記錄的信息**：
   - API端點和調用時間
   - 請求參數完整內容
   - 響應狀態碼和響應時間
   - 響應內容（成功和錯誤都要記錄）
   - 錯誤發生的精確步驟

4. **存儲路由檢查清單**：
   - 每次實現文件上傳功能時，必須同步檢查對應的存儲路由
   - 確保外部API能夠訪問上傳的文件
   - 測試文件URL的外部可訪問性

**執行標準**：
- **開發階段**：所有關鍵操作都要有詳細控制台日誌
- **調試階段**：通過控制台日誌快速定位問題
- **部署階段**：確保生產環境的日誌足夠詳細

#### M. 🚨 **六步成功修復法（基於2025-09-01經驗）**
**目的**：基於數字人克隆成功經驗，建立系統性問題解決方法

**核心原則**：**系統性解決所有基礎問題，而非單點修復**

**六步修復法**：
1. **URL外部可訪問性驗證**
   - 設置URL後立即測試外部訪問性
   - 使用 `fetch(url, {method: 'HEAD'})` 驗證
   - 不可訪問立即修復，不可帶病運行

2. **三次修改規則**
   - 超過3次修改無效立即重新設計
   - 成本對比：修改舊系統 vs 重新設計
   - 避免沉沒成本陷阱

3. **路由定義同步檢查**
   - 新URL引用必須同步檢查路由定義
   - 建立路由檢查清單
   - 使用URL常量避免硬編碼

4. **框架語法對照表**
   - 建立Webman vs Laravel語法對照表
   - 避免重複性語法錯誤
   - 建立"已知錯誤檢查清單"

5. **命名規範統一性**
   - 新開發必須遵循現有命名邏輯
   - 檢查現有環境變數前綴和格式
   - 建立命名檢查清單

6. **開發文檔同步機制**
   - 開發完成 = 代碼完成 + 文檔更新
   - 建立強制文檔同步檢查點
   - 定期文檔審查機制

**執行標準**：
- **每個開發任務必須經過六步檢查**
- **任何一步失敗立即修復，不進行下一步**
- **建立檢查清單，確保不遺漏**

#### N. 🚨 **強制終端指令目錄規範** 
**目的**：確保所有終端指令都在正確目錄執行，避免路徑混亂

**🚨 強制要求**：
1. **每個終端指令必須明確指定執行目錄**
2. **格式要求**：
   ```
   📁 執行目錄：D:\specific\directory\path
   🔧 命令：specific command here
   ```
3. **禁止模糊指令**：不能只說"執行某命令"而不指定目錄
4. **路徑驗證**：執行前必須確認當前目錄正確

**標準格式範例**：
```powershell
📁 執行目錄：D:\genhuman\genhuman\server
🔧 命令：C:\php\php.exe .\composer.phar install

📁 執行目錄：D:\genhuman\genhuman\vidspark
🔧 命令：npm run build
```

**違規範例（禁止）**：
- ❌ "執行 php start.php start"
- ❌ "運行 npm install"
- ❌ "切換目錄後執行..."

**執行標準**：
- 每次提供指令前，必須先指定📁執行目錄
- 用戶必須先cd到指定目錄再執行🔧命令
- 如果目錄錯誤，立即停止並重新指定

#### O. 🚨 **Webman框架特殊語法規則（嚴重警告）**
**目的**：避免Webman vs Laravel語法混用錯誤

**🔥 嚴重警告**：**已發生3次重複性Webman語法錯誤！**

**核心提醒**：**Webman不是Laravel，語法完全不同**

**🚨 強制預防措施（立即執行）**：
1. **每次寫Response前暫停5秒確認語法**
2. **代碼中添加強制框架提醒註釋**  
3. **建立自動檢查腳本掃描Laravel語法**
4. **重複錯誤超過3次停止開發，重新學習框架**

**關鍵語法對照**：
```php
// ❌ 錯誤（Laravel語法）
return response()->json([...], 200);

// ✅ 正確（Webman語法）
return new Response(200, [
    'Content-Type' => 'application/json; charset=utf-8'
], json_encode([...], JSON_UNESCAPED_UNICODE));
```

**強制檢查清單（每次必須執行）**：
- [ ] **暫停5秒思考**：這是Webman還是Laravel？
- [ ] **檢查語法**：確認使用`new Response()`而非`response()`
- [ ] **添加註釋**：`// 🚨 Webman語法：new Response()`
- [ ] **測試驗證**：立即測試API端點是否返回200

**緊急預防措施**：
- **開發前背誦**：Webman用`new Response()`，Laravel用`response()`
- **代碼檢查**：每個文件完成後搜索`response(`關鍵字
- **立即回滾**：發現語法錯誤立即回滾，不嘗試修復
- **學習機制**：連續3次錯誤必須停止開發24小時學習框架

### ⚠️ 重要提醒
- **第零優先級**：使用真實時間進行所有時間管理
- **第一優先級**：任務監控，防止開發卡住
- **🚨 強制控制台日誌**：所有關鍵操作都要有詳細的時間戳日誌
- **🚨 文檔生成**：必須創建真實文件，不能只提供文字內容
- **🚨 項目路徑配置**：執行命令前必須確認正確的項目路徑
- **🚨 存儲路由檢查**：文件上傳功能必須同步檢查存儲路由
- **不要部署錯項目**：確保部署到 genhuman-digital-human.zeabur.app
- **監聽目錄正確**：genhuman\genhuman 目錄
- **每次修改都要同步**：GitHub + zeabur 雙重同步
- **時間管理**：預設時間、監控執行、防止中斷
- **質量保證**：功能測試、用戶體驗驗證、結果記錄
- **🚨 頁面空白問題**：立即按診斷規則排查，避免盲目修改

---

**創建日期**: 2025-08-26
**適用項目**: genhuman系統開發
**版本**: v1.0
